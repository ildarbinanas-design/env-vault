name: ci

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/reusable-quality.yml
    with:
      source_sha: ${{ github.sha }}
      version: ci-${{ github.sha }}
